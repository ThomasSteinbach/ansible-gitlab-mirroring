git push --mirror git@gitlab.com:{{ gitlabcom_user }}/{{ item.name }}.git
{% if item.visibility is not defined or item.visibility == 'public' %}
git push --mirror git@github.com:{{ githubcom_user }}/{{ item.name }}.git
{% endif %}
